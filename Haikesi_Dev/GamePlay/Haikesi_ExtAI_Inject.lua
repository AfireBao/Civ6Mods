-- Haikesi ExtAI inject-file poll — DISABLED for MP (Gameplay io.open unavailable).
-- 联机落地：UI 事件驱动（选卡 LuaEvents.Haikesi_ExtAIPendingUI + EditBox Ctrl+V）。
-- 单机 FireTuner Stage → ExposedMembers → LuaEvents.Haikesi_ExtAIStagedUI。
print("[Haikesi ExtAI Inject] file poll disabled (MP io); use event-driven UI Ctrl+V")
